# 기본 (키 없이도 전부 동작: BM25 검색, 권한 전수 검사, 검색 평가)
numpy>=1.26
streamlit>=1.30
chromadb>=1.0          # 벡터스토어. 권한 필터를 where 절로 DB에 내려보낸다
rank-bm25>=0.2         # BM25
kiwipiepy>=0.20        # 한국어 형태소 분석. 공백 분리는 조사 때문에 BM25가 거의 동작하지 않는다

# 온프렘 모드용 (선택). 로컬 BGE-M3 임베딩.
# torch 를 함께 받으므로 설치 용량이 큽니다 (약 2.5GB + 모델 2.2GB).
# 설치하지 않으면 BM25 전용 또는 API 임베딩으로 동작합니다.
# sentence-transformers>=3.0
