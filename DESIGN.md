# DESIGN.md - 사내 온보딩 어시스턴트 디자인 시스템

> **기반**: `DESIGN-apple.md` (토큰·타이포·여백·그림자 철학의 원본)
> **참고**: 카드 018 「AI/챗봇 플랫폼」, 카드 092 「채팅/메시징 앱」
> **이 문서**: 위 셋을 정반합으로 종합한 결과. 충돌 지점과 그 해소 근거를 남긴다.

---

## 0. 한 줄 원칙

> **근거가 곧 제품이다.**

애플의 시스템은 "UI가 후퇴해서 **제품 사진**이 말하게 한다"로 요약된다.
우리 화면에는 제품 사진이 없다. 그 자리에 **인용된 조항 원문**을 놓는다.

따라서 애플이 제품 사진에만 허락한 특권을 전부 **출처 카드**에 준다.
시스템 전체에서 유일한 그림자, 가장 넓은 여백, 주변 40px 비움.
**디자인이 곧 제품의 주장이 된다.** 이 화면에서 가장 공들인 요소가 근거라는 사실 자체가 메시지다.

---

## 1. 정반합: 충돌 5건과 해소

| # | 정 (애플) | 반 (카드 018 / 092) | 합 |
|---|---|---|---|
| 1 | **단일 액센트 절대 불가침.** 두 번째 브랜드 색은 존재하지 않는다 | 018은 AI 퍼플, 092는 블루+그린. 액센트 2개 이상 | **액센트는 1개(#0066cc)로 유지.** 대신 우리에겐 애플에 없는 **상태 4종**이 있으므로 **상태색**을 별도 축으로 분리한다. 상태색은 클릭을 유도하지 않으므로 액센트가 아니다. §2 참조 |
| 2 | **그림자는 시스템 전체에 딱 하나**, 제품 사진 전용 | 챗 UI엔 제품 사진이 없음 | 애플 규칙을 그대로 쓰면 **그림자 0개**가 된다. 대신 §0에 따라 그 하나를 **출처 카드**에 이전한다 |
| 3 | **전면 타일 교대(밝음↔어둠)가 곧 구분선** | 채팅은 세로 스트림이라 타일 교대 불가 | 대화 스트림 안에서는 타일 교대를 포기하고, **말풍선 채움 유무**가 구분선 역할을 한다 (§3) |
| 4 | **호버를 문서화하지 마라.** 기본/눌림 두 상태만 | 092: 섬세한 호버 200ms | 데스크톱 호버는 **배경 틴트만 200ms**. 이동·확대·그림자 금지. 눌림은 애플의 `scale(0.95)` 유지 (§6) |
| 5 | (해당 없음) | **018: "AI 제품의 신뢰는 '응답하고 있음'을 보여주는 데서 온다"** | **이 전제는 우리에게 틀렸다.** §1.1 |

### 1.1 018의 전제를 뒤집는다 (가장 중요한 결정)

범용 AI 챗봇은 "살아 있음"이 신뢰다. 침묵이 불안이기 때문이다.
**사내 규정 어시스턴트의 신뢰는 정반대 지점에서 온다: 근거를 보여주는 것, 그리고 모를 때 멈추는 것.**
이 제품이 자랑하는 동작은 **답하지 않는 것**이다.

그러므로:
- **스트리밍 텍스트와 타이핑 인디케이터는 신뢰 신호가 아니라 지연 커버로 강등한다.**
  로컬 Ollama CPU 추론은 실제로 느리므로 기능적으로는 필요하다. 넣되, 시각적 위계에서
  **출처 블록보다 아래**에 둔다.
- 타이핑 인디케이터를 브랜드 모먼트로 키우지 않는다. 018의 "AI 네이티브 UI" 연출은 채택하지 않는다.
- 대신 **거부 상태를 가장 잘 디자인된 화면으로 만든다.** 심사 키워드 "신뢰도"가 여기 걸린다.

### 1.2 각 카드에서 실제로 가져온 것

| 출처 | 가져온 것 | 버린 것 |
|---|---|---|
| 018 | 뉴트럴 배경으로 대화에 집중시킨다는 원칙 / 스트리밍·페이드인(지연 커버로 강등) / 색 `#1E1B4B` 1개 | **AI 퍼플 전체.** 사규 도구에 퍼플 메인은 장난감처럼 읽힌다. 애플 양피지 `#f5f5f7`가 뉴트럴 배경 역할을 이미 한다 |
| 092 | **"발신/수신 대비가 유일하게 필요한 장식"** 원칙 / 호버 200ms / `#059669`, `#DC2626` | 메인 `#2563EB`. 액센트는 애플 블루로 통일 |

---

## 2. 색

### 2.1 액센트 (1개, 불가침)

`DESIGN-apple.md`의 `{colors.primary}` **#0066cc**를 그대로 쓴다.
**클릭 가능한 모든 것이 이 색이고, 이 색인 것은 전부 클릭 가능하다.**

### 2.2 표면·텍스트

`DESIGN-apple.md`에서 그대로 상속한다.

| 역할 | 토큰 | 값 |
|---|---|---|
| 캔버스 | `canvas` | `#ffffff` |
| 양피지 (사용자 말풍선, 출처 카드) | `canvas-parchment` | `#f5f5f7` |
| 잉크 | `ink` | `#1d1d1f` |
| 흐린 잉크 (메타데이터, 시행일) | `ink-muted-48` | `#7a7a7a` |
| 헤어라인 | `hairline` | `#e0e0e0` |

### 2.3 상태색 (4개, 액센트 아님)

애플 시스템에 없는 축이다. 우리 화면에는 애플이 다루지 않는 상태가 넷 있다.

| 상태 | 언제 | 색 | 출처 |
|---|---|---|---|
| **근거 확인** | 답변 + 출처 정상 | `#059669` | 092 악센트 |
| **거부** | 근거를 못 찾음 | `#B45309` (앰버) | 신규 |
| **권한 차단** | `blocked_chunks > 0` | `#1E1B4B` | 018 텍스트색 |
| **인용 오류** | `unknown_citations` 있음 | `#DC2626` | 018·092 공통 경고색 |

**적용 규칙 3가지 (애플의 단일 액센트 정신을 깨지 않기 위한 제약)**
1. 상태색은 **3px 좌측 바**와 **작은 배지**에만 쓴다. 면적을 채우지 않는다.
2. 상태색으로 **텍스트를 칠하지 않는다.** 본문은 언제나 `ink`.
3. 상태색은 **클릭 유도 신호가 아니다.** 상태 블록 안의 링크도 액센트 `#0066cc`를 쓴다.

**거부에 빨강을 쓰지 않는 이유**: 거부는 실패가 아니라 **정상 동작**이다. 이 제품이 자랑하는 동작이다.
빨강은 "고장났다"로 읽히고, 회색은 "응답 안 됨"으로 읽힌다. 앰버는 "주의해서 보라"에 가깝다.
빨강은 **인용 오류**에만 남긴다. 그건 진짜로 시스템이 틀린 경우다.

**권한 차단을 거부와 다른 색으로 두는 이유**: 사용자가 취할 행동이 다르다.
거부는 "담당 부서에 문의", 권한 차단은 "권한이 필요하다". 같은 색이면 이 구분이 사라진다.

---

## 3. 대화 레이아웃

### 3.1 말풍선 규칙 (092의 원칙을 애플식으로)

092의 **"발신/수신 색 대비가 유일하게 필요한 장식이고 나머지는 투명해질수록 좋다"**를 채택하되,
애플식으로 한 단계 더 밀어붙인다.

| 화자 | 처리 |
|---|---|
| **사용자** | `canvas-parchment` 채움, `{rounded.lg}` 18px, 우측 정렬, 최대폭 70% |
| **어시스턴트** | **말풍선 없음.** 배경과 동일, 좌측 정렬, 최대폭 680px |

**어시스턴트 답변에서 말풍선을 벗기는 이유**: 말풍선은 "누가 말했다"는 표시다.
규정 안내는 **대화가 아니라 문서에 가깝게** 읽혀야 신뢰가 생긴다.
말풍선을 벗으면 답변이 채팅 메시지가 아니라 **문서 인용**처럼 읽힌다.
이것이 "장식은 투명해질수록 좋다"의 가장 강한 형태다.

### 3.2 밀도

애플의 80px 섹션 패딩은 스크롤 스트림에 과하다.

| 대상 | 값 |
|---|---|
| 메시지 간 세로 간격 | `{spacing.lg}` 24px |
| 답변 본문 ↔ 출처 카드 | `{spacing.xl}` 32px |
| 출처 카드 주변 비움 | **40px** (애플이 제품 사진에 요구하는 최소 여백) |
| 페이지 상하 | `{spacing.xxl}` 48px |

---

## 4. 출처 카드 (이 시스템의 주인공)

애플의 `product-tile` + 유일한 그림자를 이전받는 컴포넌트다.

```
배경        canvas-parchment  #f5f5f7
모서리      {rounded.lg}      18px
패딩        {spacing.lg}      24px
그림자      rgba(0,0,0,0.22) 3px 5px 30px 0    <- 시스템 전체에서 유일
좌측 바     3px, 상태색
주변 여백   40px 이상
```

**내용 위계**
1. 문서명 + 조항 - `{typography.body-strong}` 17px/600
2. 원문 인용 - `{typography.body}` 17px/400/1.47
3. 메타데이터 (`doc_id` · 버전 · **시행일** · 등급 · 유사도) - `{typography.caption}` 14px, `ink-muted-48`

**시행일은 생략하지 않는다.** `CLAUDE.md` §4-3이 요구하는 항목이고, 낡은 규정을 최신으로
오인하는 것을 막는 유일한 장치다.

---

## 5. 상태별 화면 4종

| 상태 | 좌측 바 | 배지 | 본문 |
|---|---|---|---|
| **근거 확인** | `#059669` | `출처 N건` | 답변 + 출처 카드 |
| **거부** | `#B45309` | `근거 없음` | 폴백 문구 + **최고 유사도와 임계값을 나란히** 표시 |
| **권한 차단** | `#1E1B4B` | `권한 차단 N` | 답변 또는 거부 + 차단된 청크 수 |
| **인용 오류** | `#DC2626` | `검증 실패` | 답변 + 검증 실패한 인용 목록 |

**거부 화면에 점수와 임계값을 같이 보여주는 이유**: 이 프로젝트의 유일한 검증된 차별점이
**측정**이다 (`CLAUDE.md` §1.1). "0.34 < 임계값 0.40이라 답하지 않았습니다"는 한 줄이
거부를 감이 아니라 **측정된 판단**으로 만든다. 심사에서 이 한 줄이 다른 팀과의 차이다.

권한 차단은 **거부와 동시에 일어날 수 있다.** 두 배지를 함께 띄운다. 하나로 합치지 않는다.

---

## 6. 모션

| 대상 | 처리 |
|---|---|
| 버튼 눌림 | `transform: scale(0.95)` (애플 시스템 전역 규칙) |
| 데스크톱 호버 | 배경 틴트만, 200ms. **이동·확대·그림자 금지** |
| 스트리밍 텍스트 | 토큰 단위 출력. 지연 커버 용도이며 브랜드 모먼트가 아니다 |
| 타이핑 인디케이터 | 출처 카드보다 **아래** 위계. 크게 만들지 않는다 |
| 출처 카드 등장 | 페이드인 200ms (018에서 가져온 유일한 연출) |

**금지**: 그라디언트 배경, 카드·버튼·텍스트 그림자, 장식용 일러스트, 로딩 스켈레톤의 시각적 과장.

---

## 7. 타이포 (한글 치환)

`DESIGN-apple.md`의 계단을 그대로 쓰되 폰트만 바꾼다.

- **SF Pro → Pretendard** (`Pretendard, system-ui, -apple-system, sans-serif`)
- **본문 17px/400/1.47 유지.** 16px로 낮추지 않는다. 규정 문서는 스캔이 아니라 읽는 대상이다.
- **음수 자간은 완화한다.** 애플의 `-0.374px`를 한글에 그대로 쓰면 자소가 붙어 보인다.
  디스플레이 크기에서 **`-0.01em` 정도**로 줄이고, 14px 이하에서는 **0**으로 둔다.
- 조항 번호·`doc_id`·점수는 **`font-variant-numeric: tabular-nums`**. 숫자가 흔들리면 측정치로 안 보인다.

---

## 8. 반응형

애플 브레이크포인트를 상속하되 챗 UI에 맞게 3개만 쓴다.

| 폭 | 처리 |
|---|---|
| ≥ 1024px | 대화 스트림 680px 고정 + 우측에 검색 방식·역할 패널 |
| 640~1023px | 단일 컬럼, 패널은 상단 접이식 |
| < 640px | 사용자 말풍선 최대폭 85%, 출처 카드 주변 여백 40px → 24px |

터치 타깃 **최소 44 x 44px** (애플·092 공통). 역할 전환과 alpha 슬라이더가 여기 걸린다.

---

## 9. 하지 말 것

- **두 번째 액센트 색을 만들지 마라.** 상태색은 액센트가 아니며 3px 바와 배지 밖으로 나가지 않는다.
- **거부 화면을 에러처럼 그리지 마라.** 이 제품이 자랑하는 동작이다.
- **타이핑 인디케이터를 신뢰 신호로 쓰지 마라.** 우리의 신뢰 신호는 출처와 임계값이다 (§1.1).
- **어시스턴트 답변에 말풍선을 붙이지 마라.** 문서처럼 읽혀야 한다.
- **출처 카드 말고 어디에도 그림자를 넣지 마라.** 유일성이 깨지면 의미도 깨진다.
- **AI 퍼플을 다시 들이지 마라.** 018에서 가져온 것은 `#1E1B4B` 하나뿐이다.
- 그라디언트를 쓰지 마라. 애플 시스템에 그라디언트 토큰은 0개다.

---

## 10. 미결정

- **Streamlit 제약**: 현재 UI는 `app.py`의 Streamlit이다. 말풍선 제거, 3px 좌측 바, 유일한
  그림자, Pretendard 로딩은 커스텀 CSS 주입이 필요하다. 어디까지 Streamlit 안에서 되는지
  확인하지 않았다. **미검증.**
- **다크 모드**: `DESIGN-apple.md`도 라이트 기준이고 다크 대응은 "Known Gaps"에 남아 있다.
  해커톤 범위에서는 라이트만 한다.
- **역할 전환 UI**: 권한 데모의 핵심 조작인데(`intern` → `hr`) 어떤 컴포넌트로 둘지 미정.
  사이드바 셀렉트가 현재 구현이나, 심사 3분 시나리오에서는 더 눈에 띄어야 할 수 있다.
- 카드 018·092의 원본 캡처는 이 리포에 없다. 위 표의 색값은 캡처를 읽고 옮긴 것이다.
