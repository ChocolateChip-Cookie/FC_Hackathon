# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

이 파일은 새 세션(클로드코드 / 커서)이 이 프로젝트의 맥락을 그대로 이어받기 위한 문서입니다.
작업을 시작하기 전에 반드시 전체를 읽으십시오.

@CLAUDE-hackathon-context.md

**문서 우선순위: `CLAUDE-hackathon-context.md` > 이 파일 > 그 외 문서.**
위 컨텍스트 문서는 해커톤의 **상위 제약**(확정 주제, 과제 정의, 운영 규칙, 판단 기준, 산출물
체크리스트)이며 스스로 그렇게 선언한다. 이 파일과 충돌하면 **컨텍스트 문서가 이긴다.**
충돌하는 구현이나 제안은 **진행 전에 사용자에게 확인**한다.

위 `@import`는 매 턴 자동 로드된다. 컨텍스트 문서가 append-growing이 되면 즉시 이 import를
끊고 경로 참조로 바꾼다 (전역 R28).

---

## 0. 인계 프로토콜 (가장 먼저)

**클로드코드와 커서가 이 폴더 하나를 교대로 쓴다.** 두 도구는 서로의 대화를 볼 수 없다.
`HANDOFF.md`가 유일한 인계 채널이다.

1. **세션 시작 시 `HANDOFF.md`를 먼저 읽는다.** 그다음 `git pull`, `git log -3 --oneline --stat`로 대조.
   문서와 커밋이 어긋나면 커밋이 진실이다.
   직전 도구 태그의 마지막 커밋에 `HANDOFF.md`가 없으면 그 세션은 인계를 남기지 않은 것이다.
2. 되돌리기 어려운 판단을 했으면 `HANDOFF.md`의 "결정 로그"에 한 줄 (버린 대안 포함).
3. **끝낼 때 `HANDOFF.md`의 "현재 상태"를 덮어쓰고 "세션 로그"에 항목 1개를 추가한 뒤,
   코드 변경과 같은 커밋에 담아 push한다.** 도구를 바꾸기 직전에는 반드시 여기까지.
4. 커밋 제목 앞에 태그 `[CC]`. (커서는 `[CU]`)
5. 두 도구를 **동시에** 열지 않는다. 한쪽을 닫고 다른 쪽을 연다.

(`HANDOFF.md`는 append-growing 문서이므로 이 파일에서 `@import` 하지 않는다. 경로 참조만 한다.)

---

## 1. 프로젝트 정의

**주제·과제 정의·운영 규칙·판단 기준·산출물 체크리스트는 전부 `CLAUDE-hackathon-context.md`에 있다.**
이 파일 상단에서 `@import` 되므로 매 턴 로드된다. 여기서 다시 쓰지 않는다.
같은 규칙을 두 곳에 두면 드리프트하고, 드리프트한 규칙은 없는 규칙보다 나쁘다.

이 절에는 **컨텍스트 문서에 없는, 이 리포에만 해당하는 사항**만 남긴다.

- 더미 사규: `data/policies/` (26문서 183청크, live 179). 가상 기업 **한성산업**이며
  문서 front matter와 README에 명시한다
- On-premise 설계 문서(필수 산출물 B): `docs/onprem-design.md`
- 아래 §4(신뢰도)·§5(보안)가 평가 축 3개 중 뒤의 둘에 직접 대응한다

### 1.1 차별점: 외부 대조로 검증된 것만 주장한다

2026-08-31, 대형 RAG 리포 4종(RAGFlow 89.7k★ / Dify 153.9k★ / LangChain 145.3k★ /
Langflow 153.9k★)의 소스를 대조했다. 근거와 줄번호는 `C:\dev\REPO_REVIEW_LOG.md` "14차 배치".

**거부를 '하는 것'은 우리 차별점이 아니다. 이 전제는 유지한다.**
RAGFlow는 거부 경로를 둘 갖고 있고, 그중 에이전틱 경로
(`rag/advanced_rag/agentic_rag_graph.py`)의 LLM 충분성 판정기
(`rag/prompts/sufficiency_select.md`)가 구조화 JSON
(`coverage` / `missing_information` / `confidence` / `useful_chunk_ids`)을 뱉고
불충분하면 질의를 재작성해 재검색한다.

**흡수한 것 / 버린 것** (2026-08-31, 원문 재확인)
- **흡수**: 2겹을 `NO_ANSWER` 문자열 매칭에서 `ok` / `missing` / `use` JSON 판정으로 바꿨다.
  키 별칭으로 RAGFlow 필드명(`is_sufficient`, `missing_information`, `useful_chunk_ids`)도 읽는다.
  생성과 판정을 **한 호출**에 넣었다. RAGFlow는 판정기와 생성을 따로 부른다.
- **버린 것**: 재작성 루프. 홀드아웃 MRR 1.000이고 로컬 7B 한 호출이 35~86초다.
- **더 엄격한 것**: RAGFlow는 루프 종료 후에도 `partial_answer = True`로 잔여 근거를 답으로
  내보낸다 (`formalize_answer`). SCA 호출이 실패하면 초안을 그대로 채택한다. 둘 다 fail-open.
  우리는 불충분하면 거절로 닫는다.

**살아남은 차별점 2개**
- **권한**: 4개 리포 전부 데이터셋/KB 단위 ACL만 있다. `clearance` 코드 검색 0/4.
  **청크 단위 등급을 검색 점수 계산 안에서 적용하는 곳은 0개다.**
- **측정**: **오거부율 지표 / dev-holdout 임계값 튜닝 / 신뢰구간**, 이 3개는 4개 리포 전역
  코드 검색에서 0건이다 (`false_abstain` 0, `holdout` 0, `wilson` 0, `confidence interval` 0).

### 1.2 발표에 쓸 문구 (이 문장 그대로 쓴다)

아래 세 문장은 대조 결과에 맞춰 교정한 것이다. **왼쪽 표현은 반박당한다.**

| 축 | 쓰지 말 것 | 쓸 것 |
|---|---|---|
| 거부 | "근거 없으면 거부하는 건 우리뿐" | "RAGFlow도 거부를 한다. 충분성 JSON은 거기서 가져왔다. 다른 점은 **불충분하면 부분 답을 내놓지 않고 거절로 닫는 것**, 그리고 **거부를 측정 대상**으로 삼아 오거부율을 홀드아웃으로 튜닝한 하네스가 있는 것이다." |
| 권한 | "다른 데선 불가능" | "RAGFlow `meta_data_filter`, Dify 메타데이터 필터링으로 직접 구현은 가능하다. 다만 **기본 보안 모델로 내장한 곳이 없다.** 우리는 이걸 옵션이 아니라 기본값으로 둔다." |
| 측정 | "다른 데는 측정이 없다" | "RAGFlow는 공개 IR 데이터셋(MS MARCO / TriviaQA / MIRACL)으로 랭킹 품질을 잰다(`rag/benchmark.py`, ranx로 ndcg@10 / map@5 / mrr@10). 우리는 **자기 도메인 골든셋으로 거부 정확도까지** 잰다. 배포된 시스템이 '우리 회사 질문에 맞게 답하고 맞게 거부하는가'를 재는 하네스는 4개 중 0개다." |

보강 근거: RAGFlow 표준 거부 경로는 **옵션**이고 `similarity_threshold` 기본값 0.1은
**튜닝된 값이 아니다**. 임계값을 데이터로 정했다는 주장은 여전히 우리 쪽이 강하다.

### 1.3 이 대조의 한계 (발표에서 단정하지 말 것)

- 4개 리포 **전량 열람이 아니다**(3,600~17,000 파일). 핵심 경로 원문 열람 + 트리 전량 grep +
  전역 코드 검색의 3중 확인이며 **실제 구동은 하지 않았다.**
- RAGFlow 에이전틱 경로가 **기본값인지 옵트인인지 확정하지 못했다.**
  **"그건 옵션이다"라고 단정하지 마라.**
- 이 조사의 1차 판정은 틀렸었다("거부 1/4, 측정 0/4"). `grep` 결과를 `head`로 자른 뒤
  그것을 "없음"의 근거로 쓴 것이 원인이었다. 위 내용은 재조사본이다.

---

## 2. 스택 확정 (On-premise 원칙)

**상위 근거**: `CLAUDE-hackathon-context.md` §4 온프레미스 전제.
> "프로토타입에서 외부 LLM API를 쓰더라도, 모델/임베딩/벡터스토어는 **교체 가능한 인터페이스로
> 추상화**하고 '로컬 모델로 대체 가능'한 지점을 코드와 문서에 명시한다. **외부 의존을 몰래 늘리지 않는다.**"

즉 요구는 "외부 API 금지"가 아니라 **"추상화 + 명시"**다. 아래 2모드가 그 요구를 구현한다.

### 2.1 두 가지 모드

| 모드 | 언제 | LLM | 임베딩 | 외부 호출 |
|---|---|---|---|---|
| **온프렘 모드** (기본, 제출용) | 기본값. 심사 시연 | **Ollama 로컬** | **bge-m3 로컬** | **없음** |
| **클라우드 모드** (체험용) | 내려받은 사람이 로컬 모델 없이 돌릴 때 | Anthropic / Upstage / OpenAI | Upstage / OpenAI | 있음 |

**클라우드 모드는 더미데이터에만 쓴다. 사내 실데이터에는 쓰지 않는다.**
심사에서 "외부 API 쓰지 않았느냐"고 물으면 이 표가 그대로 답이 된다.

### 2.2 고정 스택

| 계층 | 확정 | 비고 |
|---|---|---|
| LLM | **Ollama 로컬** (1순위) | 없으면 Anthropic → Upstage → OpenAI 순으로 폴백 |
| 임베딩 | **bge-m3 로컬** (1순위) | 없으면 Upstage → OpenAI. **Anthropic은 임베딩 API가 없다** |
| 벡터스토어 | **ChromaDB (로컬)** | §2.3 참조 |
| 캐시 | Redis | 로드맵 7단계. 지금 도입하지 않는다 |
| 트레이싱 | Arize Phoenix (self-host) | 미착수 |

### 2.3 벡터스토어를 FAISS가 아니라 ChromaDB로 두는 이유

**FAISS에는 메타데이터 `where` 필터가 없다.**
권한 필터가 지금 `src/store.py`의 `dense_scores`에서
`where={"clearance": {"$in": ...}}`로 **DB 질의 조건에 내려가 있다.**
FAISS로 바꾸면 이 필터를 애플리케이션 코드로 끌어올려야 하고, 그것은 §5-1이 요구하는
"검색 단계 필터링"을 약화시킨다. **보안 축을 지키기 위해 ChromaDB를 쓴다.**

### 2.4 "몰래 늘리지 않는다"를 지키는 방법

추상화만으로는 부족하다. 지금 무엇이 쓰이는지 **보여야** 한다.

1. **백엔드 선택은 `src/providers.py` 한 곳에서만 한다.** 판정이 갈라지면 화면과 평가가 다른 말을 한다.
2. **`providers.uses_external_api()`**가 외부 경로가 열렸는지 판정하고,
   **사이드바에 현재 모드를 항상 표시한다** (`providers.mode_label()`).
   키를 갖고 있는 것과 외부로 보내는 것은 다른 일이므로 둘을 구분해 표시한다.
3. 새 외부 의존을 추가할 때는 `providers.py`의 표에 먼저 올린다. 그 표에 없는 외부 호출은 금지다.

---|---|
| LLM | **Ollama 로컬 모델** |
| 임베딩 | **bge-m3 로컬 모델** |
| 벡터스토어 | **FAISS (로컬)** |
| 캐시 | **Redis** |
| 트레이싱 | **Arize Phoenix (self-host)** |

**사내 규정 원문이 외부 API로 나가는 경로를 만들지 않는다.**
온프렘이 주제인데 임베딩이나 생성을 외부로 보내면 요건 자체가 무너진다. 심사에서 가장 먼저 찔릴 지점이다.

**개발 편의상 외부 API를 임시로 쓰는 경우**에만 예외를 허용하되, 그때는 반드시 두 가지를 남긴다.
1. **코드 주석**에 임시임을 명시하고 **전환 지점**을 표시한다.
2. **README**에 같은 사실을 적는다.

전환 지점을 남기지 않은 임시 코드는 임시가 아니라 그냥 위반이다.

---

## 3. 작업 순서 (강제)

### 3.1 코드보다 설계가 먼저

**"지금 당장 코드 작성하지 말고 설계부터 의논하자"가 기본값이다.**
새 기능이나 구조 변경은 plan mode로 설계를 먼저 논의하고, 합의된 뒤에 코드를 쓴다.
사소한 오타 수정이나 단일 값 변경은 예외다.

### 3.2 파이프라인 8단계

**문서 로드 / 텍스트 분할 / 임베딩 / 벡터스토어 저장 / 리트리버 / 리랭킹 / 프롬프트 생성 / 답변 생성**

### 3.3 구축 로드맵 10단계

**앞 단계 없이 뒷 단계로 건너뛰지 않는다.**

| # | 단계 | 내용 |
|---|---|---|
| 1 | 기본 구조 | Config 중앙화 + **보안·정책 설계(권한 모델, Rate Limit)**. 보안은 나중에 붙이는 게 아니라 여기서 설계한다 |
| 2 | 검색 기능 | 기본 Chunking(500자) → 로컬 임베딩 → FAISS → Cosine Similarity → 답변 생성. **여기까지가 최소 동작 버전** |
| 3 | 성능 측정 | NDCG@k, LLM 자동 평가, 토큰 사용량, Latency P50~P99, Tracing 설계. **측정 없이 다음으로 가지 않는다** |
| 4 | 검색 정확도 | Semantic Chunking + Overlap, Hybrid Search(BM25 + Dense + RRF), Query Rewriting, Cross-Encoder Reranking, Ablation Study |
| 5 | 정교화 | Multi-Vector Index(chunk/sent/kw), Context Compression |
| 6 | 답변 품질 | 메타데이터 저장(출처 기반), Source Attribution, Multi-doc Reasoning, Self-Refinement |
| 7 | 자동 보정 | Embedding Cache, Fallback 재검색, Failure 분류, 다계층 Cache |
| 8 | 구조 정리 | 라우터 분리(`/docs`, `/prefix`, 기능별) |
| 9 | 속도·안정성 | 병렬 검색, Rate Limit 적용, Alert |
| 10 | 고급 | Query Routing, Self-RAG, Multi-Hop Decomposition, Streaming(SSE), RAGAS 평가 |

**해커톤 제출 목표선은 6단계까지다.** 7단계 이후는 시간이 남을 때만 손댄다.

**예외 1건**: 6단계의 **"출처 기반 메타데이터 저장 + Source Attribution"**은 평가 키워드(신뢰도)에
직결되므로 **우선순위를 앞당겨 4단계와 병행해도 좋다.**

### 3.4 기준선 없는 최적화 금지

**3단계(성능 측정) 이전에 캐시, RediSearch, 그래프RAG, 멀티에이전트를 도입하지 않는다.**
측정하지 않은 상태의 최적화는 무엇이 개선됐는지 말할 수 없고, 발표에서도 근거가 되지 않는다.
개선을 주장하려면 개선 전 수치가 먼저 있어야 한다.
확정 스택에 Redis가 있어도 도입 시점은 7단계다. 스택에 있다는 것이 지금 넣어도 된다는 뜻이 아니다.

---

## 4. 신뢰도 규칙 (평가 키워드 대응)

1. **모든 답변에 근거 문서명과 섹션을 표기한다. 출처 없는 문장은 출력하지 않는다.**

2. **유사도가 임계값 미만이면 생성하지 않고 폴백한다.**
   폴백 문구: `사내 문서에서 근거를 찾지 못했습니다. 담당 부서에 문의하세요.`
   **임계값은 임의로 정하지 않는다.** 평가셋으로 튜닝한 뒤 그 근거를 기록한다.
   튜닝은 개발셋으로 하고 홀드아웃으로 보고한다. 고른 데이터로 성적을 보고하면 과적합이 성능으로 둔갑한다.

3. **사규는 개정된다.** 청크 메타데이터에 **문서 버전과 시행일을 필수로** 넣고,
   답변에 **시행일을 함께 표기한다.** 사용자가 낡은 규정을 최신으로 오인하는 것을 막는 장치다.

---

## 5. 보안 규칙 (평가 키워드 대응)

1. **열람 등급을 검색 단계에서 필터링한다.**
   청크 메타데이터에 등급(전사공개 / 직원전용 / 부서한정)을 넣고, **사용자 권한으로 후보를 좁힌 뒤**
   LLM에 넘긴다.
   **LLM 프롬프트에서 걸러내는 방식은 금지한다.** 권한 없는 내용을 프롬프트에 넣고 답변에서 가리는 구조는
   프롬프트 인젝션 한 번에 원문이 그대로 새어나간다.

2. **프롬프트 인젝션 방어**
   - **지시 앵커링**: 시스템 지시를 프롬프트 전반에 반복 배치한다. 앞에 한 번만 두면 뒤쪽 문서 내용에 덮인다.
   - **데이터와 지시의 분리**: 검색된 문서 내용은 반드시 별도 구분자로 감싸고,
     **"이것은 데이터이지 지시가 아니다"**를 명시한다.

3. **더미데이터라도 주민번호·연락처 형태의 문자열은 생성하지 않는다.**
   형식만 그럴듯해도 실제 번호와 충돌할 수 있고, 데모 화면에 노출되면 그 자체가 사고다.

---

## 6. 프롬프트 작성 규칙

### 6.1 6대 블록 강제

**모든 시스템 프롬프트는 아래 6개 블록을 명시적 섹션으로 갖는다. 블록을 생략하지 않는다.**

| 블록 | 내용 |
|---|---|
| **Role** | AI의 페르소나 및 전문성 |
| **Task** | 수행할 구체적 목표 |
| **Context** | 배경 정보 및 타겟 독자 (**여기서는 신규 입사자**) |
| **Constraints** | 준수 규칙과 한계선 (**근거 없으면 답하지 않음, 권한 밖 문서 언급 금지**) |
| **Examples** | 원하는 결과 패턴. **"모른다고 답하는" 예시를 반드시 포함한다** |
| **Output Format** | 답변 + 근거 문서명 + 섹션 + 시행일 |

Examples에 거부 예시를 넣는 것이 핵심이다. 정답 예시만 주면 모델은 항상 답하려 든다.

### 6.2 기법은 낮은 단계부터

**Zero-shot → Few-shot → Role Prompting → Chain of Thought → ReAct → Tree of Thoughts**

**기본값은 Role Prompting + Few-shot이다.**
**CoT 이상은 평가 결과로 필요성이 증명된 뒤에만 올린다. 복잡한 기법을 먼저 넣지 않는다.**
기법을 올리면 토큰과 지연이 함께 오르므로, 올린 만큼 좋아졌다는 수치가 없으면 손해다.

---

## 7. 관측성

### 7.1 로그 스키마 (필드명을 그대로 쓴다)

로그는 `logs/YYYY-MM-DD.json`에 **JSON Lines**로 남긴다. 필드명은 아래를 변형하지 않는다.

```
latency_ms, ttft_ms, input_tokens, output_tokens, tokens_per_second,
estimated_cost_usd, avg_retrieval_score, answer_length, cache_hit
```

### 7.2 트레이싱

**Arize Phoenix를 self-host로** 띄운다. 연동은 최소화한다.
트레이싱 코드가 애플리케이션 코드에 스며들기 시작하면 그때부터 유지보수 대상이 된다.

**결정: 트레이싱을 얻자고 검색 경로를 LangChain으로 옮기지 않는다.** (2026-08-31 확정)
`libs/core/langchain_core/vectorstores/base.py:1052`에서 LangChain retriever는
`docs = [doc for doc, _ in docs_and_similarities]`로 **점수를 버린다.** 임계값 미달이어도
`logger.warning`만 찍고 `[]`를 반환하므로(`base.py:542-553`) 호출자는 **"근거가 약함"과
"문서가 아예 없음"을 구분할 수단이 없다.**
우리 거부 1겹은 `max_score`에 의존한다. 검색을 LangChain retriever로 갈아끼우면 차별점이 죽는다.

따라서 `LangChainInstrumentor` 경로는 배제한다. **LangChain 없이 Phoenix를 붙이는 방법
(수동 OTel span, `openinference-instrumentation-anthropic` 등)은 아직 조사되지 않았다.
착수 전에 확인할 것.**

**Loki / Promtail / Grafana 스택은 해커톤 범위에서 제외한다.**
설계 문서에 "확장 시 이렇게 붙인다"로만 기술한다.

### 7.3 진단 순서

증상별로 이 순서를 따른다. 아무 데나 먼저 보지 않는다.

| 증상 | 확인 순서 |
|---|---|
| 느리다 | `ttft_ms` → `cache_hit` |
| 비용이 높다 | `estimated_cost_usd` → `input_tokens` |
| 답변이 엉뚱하다 | `avg_retrieval_score` 먼저 → 그 다음 프롬프트 렌더링 결과 |
| 에러 | Phoenix에서 `RunnableSequence` trace로 끊긴 지점 확인 |

"답변이 엉뚱하다"에서 검색 점수를 먼저 보는 이유는, 대부분의 엉뚱한 답이 생성 문제가 아니라
**검색이 엉뚱한 문서를 준 결과**이기 때문이다. 프롬프트부터 고치면 원인을 지나친다.

---

## 8. 평가

1. **LLM-as-a-Judge 4요소**를 1~5점으로 평가하고 `logs/evals/`에 저장한다.
   **관련성 / 충실도 / 완전성 / 명확성**

2. **"답변 거부가 필요한 질문"을 평가셋에 반드시 포함한다.**
   사규에 없는 질문, 권한 밖 질문. 온보딩 도메인은 신입이 모르는 것을 묻는 자리라 과잉 답변의 피해가 크다.
   **정답률만 보면 과잉 답변을 못 잡는다.** 전부 답하는 시스템도 정답률은 높게 나온다.

3. **소수 지표 과적합을 경계한다.** 지표는 4계층으로 나눠 본다.
   **인프라 / 파이프라인 / 출력품질 / 사용자피드백**
   한 숫자를 올리는 데 몰두하면 나머지 셋이 조용히 나빠진다.

4. 검색 지표는 **NDCG@k**, 응답 지표는 **Latency P50~P99**와 토큰 사용량을 함께 본다 (로드맵 3단계).
   RAGAS는 10단계 항목이므로 목표선(6단계) 안에서는 쓰지 않는다.

5. 표본이 작을 때는 **비율만 말하지 않는다.** 표본 수와 신뢰구간을 함께 적는다.
   n이 작으면 비율 하나는 측정이 아니라 일화다.

---

## 9. Kafka 결정 (확정)

**결정: 프로토타입에 Kafka를 구현하지 않는다. On-premise 설계 문서에만 기술한다.**

구현하지 않는 이유:
1. 동시 사용자 수십 명 규모에서 Kafka의 강점(높은 처리량, 수평 확장)이 **발동할 조건이 없다.**
2. 비동기 구간이 생기면 **Phoenix 트레이스 기반 진단 경로가 끊긴다.**
   관측성이 이 주제의 강점 축인데 이를 약화시킨다.
3. 브로커 운영 부담이 **시연 리스크**를 키운다. 해커톤 목표는 상용 완성도가 아니다.

설계 문서(`docs/onprem-design.md`)에 담을 내용과 도입 판단 기준선은 그 문서의 §Kafka 절에 있다.
이 결정을 뒤집으려면 그 문서의 기준선 표에서 조건이 성립함을 먼저 보인다.

---

## 10. 금지 사항

1. **참고 문서의 은행 도메인 임계값을 근거 없이 복사하지 않는다.**
   환각률 1%, 유사도 0.92, TTL 1시간 같은 값은 그 도메인과 그 데이터에서 나온 숫자다.
   **우리 데이터로 측정한 뒤 정한다.**

2. **NotebookLM이 생성한 슬라이드에는 실재하지 않는 API 이름이 섞여 있다.**
   **코드 시그니처는 문서가 아니라 라이브러리 공식 문서에서 확인한다.**
   문서에서 본 함수명을 그대로 호출하지 않는다.

3. **참고 자료의 Kafka 내용은 전부 Java 클라이언트 기준이다.**
   Python으로 구현할 경우 API 이름과 테스트 도구(`MockAdminClient` 등)가 그대로 대응되지 않는다.
   **설계 문서에 Java 코드를 그대로 옮기지 말고 개념 수준으로 기술한다.**

4. **검증하지 않은 것을 검증했다고 쓰지 않는다.**
   완료 보고에는 항상 세 가지를 함께 쓴다:
   **무엇을 했는지 / 어떻게 확인했는지 / 확인 못 한 것.**
   실행하지 않은 코드에 "테스트했다"고 쓰지 않는다.

5. 원인을 짚기 전에 고치지 않는다. 증상 패치 금지.

6. 같은 방법이 두 번 실패하면 세 번째를 시도하지 않는다. 멈추고 무엇이 어디서 깨졌는지 보고한다.

7. 요청 범위를 넘겨 확장하지 않는다.

8. 한국어로 답한다. em dash를 쓰지 않는다.

---

## 11. 현재 구현 상태

> 이 절은 사실만 적는다. 확인하지 않은 것은 "미검증"으로 남긴다.

**검증된 것** (명령을 실제로 돌려 확인함)

> 2026-09-01 코퍼스 마이그레이션 이후 값이다. 그 이전 수치(7문서 32청크, 골든셋 40문항,
> `intern` 역할, 임계값 0.56)는 **전부 폐기됐다.** git 히스토리에만 남는다.

- 더미 사규 **26문서 183청크**(live 179, superseded 4 제외), `## 제N조` 헤딩 단위.
  등급 분포 public 90 / internal 46 / confidential 43.
- **권한 필터 전수 검사 통과**: live 179 x 페르소나 5 = **895조합, 반례 0건** (bm25 경로).
  페르소나는 `config.USERS` (dev / dev_lead / hr / fin / audit).
  차단 수 54 / 48 / 36 / 41 / 48. dense 경로 전수는 이 코퍼스 이후 미실행.
- **검색 방식 3종 비교** 골든셋 **107문항**.
  유형 축: normal 54 / dept 24 / permission 14 / trap 12 / conflict 3.
  기대 축: answer 70 / abstain 37. **두 축은 같은 107을 다르게 나눈 것이다.**
  개발 54 / 홀드아웃 53 (유형 축으로 층화). BGE alpha 0.8:
  dense 48/53, bm25 41/53, **ensemble 49/53**. 신뢰구간이 겹치므로 우열을 단정하지 않는다.
- **BM25 임계값은 백엔드와 무관하다.** `config.BM25_THRESHOLD` 한 곳에서만 정한다.
  백엔드별로 적어두었더니 bge/upstage만 0.69로 올리고 나머지가 0.47에 남아,
  **키 0개 사용자만 함정 12건 중 3건밖에 거부하지 못했다** (0.69면 10/12).
- **제공자 능력 매트릭스** (`python src/providers.py` 자체 점검 통과):
  앤트로픽만 -> bm25 전용 / 키 0개 -> bm25 전용 / upstage·openai -> 3방식 전부.
- `.env.local`이 `.gitignore`에 걸림 (`git check-ignore -v`로 확인).
- **LLM 생성 경로 실행됨** (Ollama CPU, `OLLAMA_NUM_GPU=0`, `qwen2.5:7b`).
  거부 3겹이 층을 이루는 것 확인: 1겹(임계값 미달), 2겹(충분성 JSON `ok:false`),
  3겹(인용 대조). 전체 평가 BGE+Ollama 93/107은 **당시 임계값 0.56 기준**이다.
- **UI**: `python -m streamlit run app.py` HTTP 200. 사이드바 모드 라벨 표시.
  출처 카드 안에 조항 원문. 지표 3개는 사이드바 하단.

**§2 확정 스택 대비 현황**

| 계층 | §2 확정 | 현재 코드 | 상태 |
|---|---|---|---|
| LLM | Ollama 1순위 | `src/llm.py`가 Ollama -> Anthropic -> Upstage -> OpenAI 순 | 구조 일치. **이 머신에서 qwen2.5:7b CPU 실동작 확인** |
| 임베딩 | bge-m3 1순위 | `src/embeddings.py` (bge / upstage / openai / hash) | 일치 |
| 벡터스토어 | ChromaDB | ChromaDB (`src/store.py`) | 일치 (§2.3에서 FAISS 대신 채택) |
| 캐시 | Redis | 없음 | 로드맵 7단계이므로 지금은 정상 |
| 트레이싱 | Phoenix self-host | 없음 | 미착수 |

**§2.4 "몰래 늘리지 않는다" 이행 상태**: `src/providers.py`가 백엔드 선택을 단일화했고
`uses_external_api()` / `mode_label()`이 `app.py` 사이드바에 연결되어 있다.

**로드맵과 현재 코드의 어긋남** (건너뛴 것이 아니라 순서가 뒤섞인 것이므로 기록만 남긴다)
- 청킹: 로드맵 2단계는 500자 고정이나 현재는 조항 헤딩 단위다. 조항 번호를 그대로 출처로 쓰기 위한
  선택이었고 4단계의 Semantic Chunking에 가깝다. 되돌릴지 유지할지는 미결정.
- Hybrid Search: 로드맵 4단계 항목이나 이미 dense + BM25 가중합으로 구현돼 있다.
  다만 **RRF가 아니라 점수 가중합**이라 로드맵 명세와 방식이 다르다.

**미검증 / 미착수**
- **골든셋 전체 LLM 평가(`evaluate.py` 기본 모드)는 한 번도 안 돌렸다.** 표본 경로(연차 / 창립기념일 / G36)만
  확인했다. §8 LLM-as-a-Judge는 미착수.
- **`logs/` 디렉터리가 없다.** §7.1의 로그 스키마는 아직 어디서도 기록되지 않는다.
- **현재 코드는 LangChain을 경유하지 않는다.** 임베딩·검색·LLM 호출을 직접 구현했다.
  **이것은 이제 결함이 아니라 의도된 선택이다** (§7.2 결정 참조). 남은 미결은
  "LangChain 없이 Phoenix를 어떻게 붙이는가" 하나다.
- **`ENSEMBLE_ALPHA = 0.8`은 개발셋 분리도로 확정했다.** (`evaluate.py --alpha-scan`)
- Phoenix 서버 패키지(`arize-phoenix`) 설치 여부 미확인.
  `arize-phoenix-otel`과 `openinference-instrumentation-langchain`은 설치돼 있다.
- **이 개발 머신에서는 `streamlit 1.56.0`으로 HTTP 200을 확인했다.**
  `requirements.txt`에는 상한을 걸지 않는다. 오염된 환경의 `fastapi`/`starlette` 충돌 우회는 README.
  **프로젝트 전용 venv가 정답이다.**
- **개인정보 마스킹은 코드에 없다.** `docs/onprem-design.md` §4.4에 설계만 있다.
  더미데이터에 주민번호·연락처 형식을 생성하지 않는 것으로 대체하고 있다.
- Pretendard 웹폰트 미로드. 발표 자료 없음.

---

## 12. 실행

```bash
pip install -r requirements.txt

python src/ingest.py                        # 문서 적재 및 색인
python eval/permission_matrix.py            # 권한 전수 검사
python eval/evaluate.py --compare           # 검색 방식 비교 (LLM 비용 0)
python eval/evaluate.py --sweep --mode ...  # 임계값 튜닝 (개발셋 -> 홀드아웃)
streamlit run app.py
```

**이 개발 머신(Windows)에서는 `python3`가 없다.** `python`(3.10.11)을 쓴다.
PowerShell에는 `export`와 `VAR=x cmd` 문법이 없다. `$env:NAME="..."`로 설정한다.
Git Bash에서는 위 문법이 그대로 동작한다.

---

## 커서 쪽 대응 파일

`.cursor/rules/handoff.mdc`가 §0의 인계 프로토콜을 담고 있다.
**한쪽을 고치면 다른 쪽도 같은 자리에서 고친다.** 두 파일이 다른 말을 하기 시작하면
프로토콜이 아니라 분쟁거리가 된다.
