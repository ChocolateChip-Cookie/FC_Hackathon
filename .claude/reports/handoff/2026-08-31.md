# 핸드오프 2026-08-31

## 세션 1 (16:58)

### 1. 메타데이터

- 일시: 2026-08-31 16:58
- 브랜치: `main`
- HEAD: `9f78319`
- 작업 트리: **dirty**. 미커밋 변경 5건 (`README.md`, `app.py`, `requirements.txt`, `src/config.py`, `src/llm.py`)
- 미추적 파일 3건 (전부 죽은 중복본): `CLAUDE (2).md`, `golden_set.json`, `onprem_architecture.md`
- 이 문서: `.claude/reports/handoff/2026-08-31.md`

**아래 라인 번호는 전부 `9f78319` + 위 미커밋 변경 기준이다.** 커밋되지 않았으므로
`git stash` 등으로 작업 트리를 되돌리면 `src/llm.py`와 `src/config.py`의 라인 번호가 어긋난다.

### 2. 현재 목표

AX 해커톤 주제 01 / Case 1, **사내 온보딩 RAG 챗봇**을 만든다. 배포하지 않고 깃헙에서
내려받아 로컬 실행하는 데모이며, On-premise 구축 방안은 문서로 산출한다.
상위 제약은 `CLAUDE-hackathon-context.md`이고 `CLAUDE.md`가 이를 `@import` 한다.

**목표가 방금 이동했다.** 세션 말미에 사용자가 "청크와 역할을 대기업 혹은 중견기업 수준으로
더미데이터 확대"를 요청했고, 규모·권한모델 질문을 **사용자가 거부하고 재질문을 요구한 상태**에서
세션이 끝났다. 즉 **다음 작업은 그 요구사항을 먼저 명확히 하는 것**이지 코드가 아니다.

### 3. 완료

- **LLM 생성 경로 최초 실행 성공** (이 프로젝트에서 처음) - 검증: `ask('연차는 사용 며칠 전에 신청해야 하나요?','intern')` → 86.2초, 답변 `3영업일 전까지 신청해야 합니다. [휴가 및 근태 규정 / 제2조]`, `unknown_citations` 빈 배열
- **거부 3겹이 실제로 층을 이루는 것 확인** - 검증: 5경로 실행 →
  권한차단 3.2초/1겹, 임계값거부 0.2초/1겹(둘 다 LLM 미호출),
  **함정 G36 36.4초·G37 35.3초가 최고점 0.763/0.673으로 1겹을 통과한 뒤 2겹(LLM NO_ANSWER)에서 거부됨**
- **3겹(인용 검증)도 실제로 작동** - 검증: 정상 답변 케이스에서 모델이 `[출처 1]`로 인용 →
  `unknown_citations: ['출처 1']`로 검출됨. 3겹이 잡은 것은 맞지만 동시에 프롬프트 결함이다
- **Ollama CUDA 실패 진단 및 우회** - 검증: `curl /api/chat` → `{"error":"...CUDA error: device kernel image is invalid"}`,
  `num_gpu:0` 추가 후 동일 요청 성공(13.6초, 8.3 토큰/초)
- **`providers._ollama_up()` 버그 수정** - 검증: 수정 전 모델 0개인데 `can_generate: True`,
  수정 후 `can_generate: False` + 안내문 `ollama pull qwen2.5:7b`. 모델 수신 후 `onprem: True`로 전환 확인
- **`ENSEMBLE_ALPHA` 0.5 → 0.8 측정으로 확정** - 검증: `evaluate.py --alpha-scan` →
  MRR은 모든 alpha에서 1.000(변별력 0), 분리도 0.727(α=0)→0.836(α=0.8~1.0)
- **`eval/evaluate.py`에 MRR·분리도·`--alpha-scan` 추가** - 검증: `--compare` → MRR 열 출력,
  홀드아웃 19문항 dense 14 / bm25 14 / ensemble 15
- **Streamlit 기동 복구** - 검증: `streamlit<1.57` 설치 후 `python -m streamlit run app.py` → HTTP 200
- **UI 첫 렌더링 확인** - 검증: `npx playwright screenshot --wait-for-timeout=45000` →
  데모 배너, 사이드바 모드 라벨 `온프렘 모드 · 로컬 BGE-M3 + Ollama · 외부 API 호출 없음`, 역할/방식/슬라이더 모두 표시됨
- **`streamlit<1.57` 상한을 `requirements.txt`에서 제거** - **[미검증]** 깨끗한 venv에서
  실제 설치를 돌려보지 않았다. 근거는 메타데이터 조사뿐(`chromadb → fastapi`는 `extra=='dev'`)
- **`app.py` 슬라이더 기본값 버그 수정**(0.5 하드코딩 → `ENSEMBLE_ALPHA`) - **[미검증]** 수정 후 화면 재촬영 안 함

### 4. 핵심 결정

- **`streamlit<1.57` 상한을 걸지 않는다.** 충돌 원인은 `fastapi 0.115.6 → starlette<0.42`인데
  **`fastapi`는 이 프로젝트 의존성이 아니다**(chromadb가 `dev` 익스트라로만 요구, 우리는 임베디드
  `PersistentClient` 사용). 오염된 환경에서만 나는 문제로 전원을 낡은 버전에 묶지 않는다.
  대신 README에 증상과 우회법을 적었다. **기각한 대안**: 상한 유지(사용자가 "심사위원이 버전을
  못 고른다"고 지적했고, 깨끗한 venv에서는 애초에 충돌이 없으므로 상한이 불필요)
- **CPU 강제를 코드에 넣지 않는다.** `OLLAMA_NUM_GPU` 환경변수로 빼고 이 머신은 `.env.local`에서
  `0`으로 설정. **기각한 대안**: `llm.py`에 `num_gpu:0` 하드코딩(GPU가 정상인 환경까지 CPU로 묶고,
  `docs/onprem-design.md` §3.2의 GPU 서빙 설계와 모순)
- **alpha는 개발셋 분리도로 고른다.** MRR이 천장(1.000)이라 변별력이 없었다.
  **기각한 대안**: 홀드아웃 정답률로 alpha 선택(α=0.5가 16/19로 1문항 높았으나, 홀드아웃으로
  되고르면 그것은 더 이상 홀드아웃이 아니다. 1문항은 n=19에서 잡음)
- **벡터스토어는 FAISS가 아니라 ChromaDB.** FAISS에는 메타데이터 `where` 필터가 없어
  권한 필터를 애플리케이션 코드로 끌어올려야 한다. **성능이 아니라 보안 때문**
- **해커톤 컨텍스트를 `@import`로 고정.** 커서에는 `.cursor/rules/hackathon-context.mdc`가
  원문을 **읽도록 지시**한다. **기각한 대안**: 커서 규칙에 내용 복사(두 벌이 되면 드리프트)

### 5. 함정

**막다른 길 (이미 시도해서 실패함)**

- **Vercel 배포**: Streamlit 미지원 + torch 495MB·BGE-M3 2.2GB가 Python 번들 상한 500MB 초과. 구조적 불가
- **starlette 단독 업그레이드**: `fastapi 0.115.6`이 `<0.42.0`으로 핀. 깨진다
- **Claude in Chrome으로 UI 확인**: 확장 미연결. **Playwright(`npx playwright`, 1.62.1 전역 설치)가 동작하므로 그쪽을 쓸 것**
- **Ollama 기본 설정(GPU)**: `CUDA error: device kernel image is invalid`로 HTTP 500. `num_gpu:0` 필요
- **8초 대기 스크린샷**: 앱이 `sentence_transformers` 임포트로 초기화 중이라 빈 화면. **45초 필요**

**깨면 안 되는 것 (불변조건)**

- **권한 필터는 chroma `where` 절에 내려가 있다** (`src/store.py`의 `dense_scores`).
  애플리케이션 코드로 올리면 `CLAUDE.md` §5-1 위반
- **`data/policies/*.md` 본문 맨 앞에 줄을 추가하면 청킹이 깨진다.** `src/ingest.py`의
  `re.split(r"\n(?=## )")`가 그 줄을 별도 청크로 만들어 32청크 기준이 무너진다.
  가상회사 표기를 front matter에만 넣은 이유가 이것이다
- **BM25 점수는 `raw/(raw+K)` 고정 포화 변환이다** (`src/bm25.py`). 질의별 min-max 정규화로
  바꾸면 최고점이 항상 1.0이 되어 **거부 임계값이 무력화**된다
- **`solar-embedding`은 비대칭 모델**이다. 색인은 `-passage`, 질의는 `-query`.
  뒤바꿔도 에러가 안 나고 점수만 조용히 나빠진다 (`src/embeddings.py`의 `is_query`)
- **임베딩 백엔드가 바뀌면 인덱스를 다시 만들어야 한다.** 벡터 공간이 달라 섞을 수 없다
- **`.env.local`은 커밋 금지.** `.gitignore:9`에 있고 현재 `OLLAMA_NUM_GPU=0`이 들어 있다

### 6. 관련 파일

- `src/providers.py:41` `_ollama_ready()` - 서버 응답만으로 준비됨을 판정하면 안 되는 이유가 여기 있다
- `src/providers.py:110` `capabilities()` - UI와 CLI가 공유하는 단일 판정. 여기가 갈라지면 화면과 평가가 다른 말을 한다
- `src/config.py:55` `ENSEMBLE_ALPHA = 0.8` - 선택 근거가 주석에 있다
- `src/config.py:70` `ABSTAIN_THRESHOLD["bge"]` - α=0.8 기준으로 개발셋 스윕한 값
- `src/config.py:109` `OLLAMA_NUM_GPU` - `None`이면 Ollama가 알아서 정한다
- `src/llm.py:45` Ollama `num_gpu` 분기 - 설정된 경우에만 보낸다
- `src/answer.py:16` `SYSTEM` - **`CLAUDE.md` §6의 6대 블록 구조가 아니다.** 개정 대상
- `src/answer.py:34` `_validate_citations()` - 3겹. 실제로 `[출처 1]`을 잡아냈다
- `src/answer.py:62` `if "NO_ANSWER" in raw` - 2겹. 모델이 설명조로 쓰면 오탐 가능
- `eval/evaluate.py:168` `separation()` - alpha 선택 기준. 임계값과 무관하다
- `eval/evaluate.py:111` `reciprocal_rank()` - 거부 문항은 `None`(MRR 오염 방지)
- `app.py:81` alpha 기본값 - `ENSEMBLE_ALPHA`를 읽는다
- `CLAUDE-hackathon-context.md` - **상위 제약.** `CLAUDE.md`보다 우선한다

### 7. 남은 작업

- **미커밋 변경 5건이 작업 트리에 있다.** `README.md`(핀 제거 + 증상 문서화), `requirements.txt`(상한 제거),
  `src/config.py`·`src/llm.py`(`OLLAMA_NUM_GPU`), `app.py`(슬라이더 기본값). 회귀 확인은
  `providers.py` 자체 점검과 LLM 5경로 실행으로 했으나 `--compare`·`permission_matrix`는 이 변경 후 재실행하지 않았다
- **`src/answer.py:16`의 `SYSTEM`이 `CLAUDE.md` §6의 6대 블록 구조가 아니다.**
  Role/Task/Context/Constraints/Examples/Output Format이 없고 특히 **"모른다고 답하는 예시"가 없다**.
  §5-2의 지시 앵커링과 "데이터이지 지시가 아님" 명시도 빠져 있다.
  기준선(현재 프롬프트) 측정은 5경로 실행으로 일부 확보됐으나 골든셋 전체 모드는 안 돌렸다
- **답변에 시행일이 없다.** `CLAUDE.md` §4-3이 요구하는데 프롬프트가 강제하지 않는다.
  출처 카드(`app.py:112` `render_sources`)에는 시행일이 들어간다
- **모델이 `[출처 1]` 형식으로 인용한다.** 프롬프트는 `[문서명 / 섹션]`을 요구하지만 지키지 않았다.
  3겹이 잡아내므로 안전하지만 사용자에게 "검증 실패" 배지가 뜬다
- **`eval/evaluate.py` 전체 모드(LLM 호출)를 한 번도 안 돌렸다.** 질문당 35~86초이므로
  답변 가능 23문항이면 15~35분 예상. 2겹 효과의 정량 수치가 여기서 나온다
- **README의 수치가 최신이 아닐 수 있다.** 3방식 비교는 α=0.8 기준으로 갱신했으나
  LLM 경로 결과(3겹 실증)는 아직 "아직 검증되지 않은 것" 절에 남아 있다
- **죽은 중복본 4건이 정리되지 않았다**: `CLAUDE (2).md`, 루트 `golden_set.json`,
  루트 `onprem_architecture.md`(미추적), `docs/onprem_architecture.md`(**추적 중**, 내용은
  `docs/onprem-design.md`로 병합 완료, 현재 구현과 어긋나는 문장 포함)
- **Pretendard 웹폰트가 로드되지 않는다.** `app.py`의 CSS가 `font-family`만 선언하고 폰트 파일을
  받지 않는다. CDN 링크는 온프렘 원칙과 충돌하므로 단순 추가가 답이 아니다. `DESIGN.md` §7 미충족
- **디스크 여유 11GB (98% 사용).** 큰 설치 전에 정리가 필요할 수 있다

**열린 질문 (사람의 답이 필요)**

- **"대기업 수준 더미데이터"의 정의.** 사용자가 요청했고 규모·권한모델 질문을 **거부하며 재질문을
  요구한 상태로 세션이 끝났다.** 확인할 것: (a) 문서 **양**인지 조직 구조 **복잡도**인지 문서 종류
  **다양성**인지, (b) 권한을 등급 1차원에서 **등급 x 부서 2차원**으로 바꿀지, (c) 역할을
  직급(사원~부장)까지 세분화할지
- 현재 권한 모델은 **1차원 계층**이다(`intern ⊂ employee ⊂ hr`, `src/config.py`의 `ROLE_CLEARANCE`).
  대기업에서는 "인사팀도 재무 기밀은 못 본다"를 표현할 수 없다. 부서 축을 넣으면
  `config.py` / `store.py` / `eval/permission_matrix.py` / 골든셋 permission 문항이 **전부 함께 바뀌고**
  검증된 "96조합 반례 0건"도 다시 재야 한다
- 문서를 늘리면 **README의 현재 수치가 전부 옛날 값이 된다.** 특히 MRR 1.000은 32청크라 쉬워서일
  가능성이 높아 떨어질 것이다. 그게 더 정직한 수치지만 "나빠진 것처럼" 보일 수 있다
- Ollama 한국어 모델을 `qwen2.5:7b`로 확정할지 미정. 비교 평가하지 않았다

### 8. 새 채팅용 프롬프트

```
C:\dev\FC_Hackathon 에서 작업한다. 브랜치 main, HEAD 9f78319 (작업 트리 dirty, 미커밋 5건).

AX 해커톤 주제 01 / Case 1 사내 온보딩 RAG 챗봇이다. 배포하지 않고 깃헙에서 내려받아
로컬 실행하는 데모이며, On-premise 구축 방안은 문서로 산출한다.

먼저 CLAUDE-hackathon-context.md 를 읽어라. 이것이 상위 제약이고 CLAUDE.md 보다 우선한다.
그다음 HANDOFF.md 와 .claude/reports/handoff/2026-08-31.md 를 읽어라.

직전 세션의 마지막 상황: 사용자가 "청크와 역할을 대기업 혹은 중견기업 수준으로 더미데이터를
만들고 싶다"고 요청했고, 규모와 권한모델을 묻는 질문을 사용자가 거부하며 재질문을 요구한
상태에서 끝났다. 따라서 첫 작업은 코드가 아니라 그 요구사항을 명확히 하는 것이다.

첫 행동: git status 로 미커밋 5건을 확인하고, python src/providers.py 와
python eval/permission_matrix.py bm25 를 돌려 현재 상태가 문서와 일치하는지 확인해라.

나열된 파일을 실제로 Read 도구로 읽고 이 문서의 주장을 코드와 대조해 검증해
```

## 세션 2 (17:50)

인계의 정본은 리포 루트 `HANDOFF.md`다. 이 파일은 상세 핸드오프용 부록이다.

### 1. 메타데이터

- 일시: 2026-08-31 17:50
- 브랜치: `main`
- HEAD: `4748ecf` 위에 dirty (이 세션이 커밋할 예정)
- 이 문서: `.claude/reports/handoff/2026-08-31.md`

**아래 라인 번호는 커밋 직전 작업 트리 기준이다.**

### 2. 현재 목표

사내 온보딩 RAG 챗봇. 배포하지 않고 로컬 실행. 상위 제약 `CLAUDE-hackathon-context.md`.

### 3. 완료

- 2겹을 RAGFlow 형식 JSON(`ok`/`missing`/`use`)으로 교체, 불충분하면 거절 - 검증: G36 `ask(...,'employee')` → 최고점 0.763, `missing: ['야근 식대 한도']`, abstained True
- 신입 연차 생성 경로 - 검증: 답변에 `[휴가 및 근태 규정 / 제2조 (연차 사용 신청)] (시행일 2026-03-01)`
- 창립기념일 1겹 - 검증: 0.374 < 0.56
- 인용 형식 자체 점검 - 검증: `python eval/test_citations.py` → 자체 점검 통과
- 출처 카드 본문을 카드 안 + 사이드바 지표 - 검증: 브라우저 `.source-card` 4장, 지표 3개 `inSidebar: true`
- Streamlit HTTP 200, Ollama `qwen2.5:7b` 사용 가능 - 검증: `python src/providers.py`

### 4. 핵심 결정

- 충분성 JSON 흡수 / 재작성 루프 기각 / `partial_answer` fail-open 기각. 한 호출에 판정+생성.
- Chainlit·kotaemon·rag-web-ui·basechat UI 교체 기각.
- `OLLAMA_NUM_GPU`는 환경변수. streamlit 상한은 requirements에 걸지 않음.

### 5. 함정

- 이 머신 Ollama 기본 GPU는 CUDA 500. `.env.local`에 `OLLAMA_NUM_GPU=0`.
- `[출처 N]` 헤더는 모델이 그대로 베낀다. 헤더를 `[문서명 / 섹션]`으로 맞춰야 3겹이 산다.
- Streamlit 다크 테마는 양피지 카드 위 글자를 밝게 뒤집어 제목이 사라진다. 카드 안 색 고정.

### 6. 관련 파일

- `src/answer.py:134` `parse_verdict()` - RAGFlow 키 별칭 포함
- `src/answer.py:26` `SYSTEM` - 6대 블록 + JSON 예시
- `src/config.py:55` `ENSEMBLE_ALPHA = 0.8`
- `src/config.py:109` `OLLAMA_NUM_GPU`
- `src/llm.py:45` `num_gpu` 분기
- `app.py:121` `render_sources()` - 본문이 카드 안
- `app.py:204` `last_stats` - 사이드바 지표
- `HANDOFF.md` - 인계 정본

### 7. 남은 작업

- 골든셋 전체 LLM 평가 없음. 대기업 더미데이터 정의는 사람 답 대기.
- 발표 자료 없음. 죽은 중복본 3건과 `docs/onprem_architecture.md` 미삭제.
- Pretendard 미로드. bge 외 임계값 미측정. Phoenix 미착수.

**열린 질문**: 더미데이터 확대가 양인지, 조직 복잡도인지, 등급×부서 2차원인지.

### 8. 새 채팅용 프롬프트

```
C:\dev\FC_Hackathon 에서 작업한다. 브랜치 main. 인계 정본은 HANDOFF.md 다.

AX 해커톤 주제 01 / Case 1 사내 온보딩 RAG 챗봇. 먼저 CLAUDE-hackathon-context.md 를 읽고,
HANDOFF.md 현재 상태와 맨 위 세션 로그를 읽어라.

첫 행동: git status 와 git log -3 --oneline --stat 으로 인계 문서와 커밋을 대조해라.
직전 [CU] 커밋에 HANDOFF.md 가 없으면 그 세션은 인계를 남기지 않은 것이다.

나열된 파일을 실제로 Read 도구로 읽고 이 문서의 주장을 코드와 대조해 검증해
```

