__pycache__/
*.pyc
data/index.npz
data/access_log.jsonl
data/chroma/

# API 키. .env.local.example 만 커밋한다.
.env
.env.local
.env.*.local
.envrc
**/.credentials.json
credentials.json
secrets.json
*.pem
*.p12
*.pfx
id_rsa
id_ed25519

# 로컬 드롭 폴더. 사규 원본은 data/policies 로 옮긴다. git add . 에 섞이지 않게 한다.
/files/

# 죽은 중복본. 커밋하지 않는다.
/CLAUDE (2).md
/golden_set.json
/onprem_architecture.md

# 로컬 에이전트 작업 문서. 클로드/커서가 이 머신에서 읽고, 깃헙에는 올리지 않는다.
DESIGN-apple.md
.claude/reports/
.cursor/rules/

# 세션 로컬 상태
.claude/settings.local.json
.claude/sessions/
.evidence/
logs/
